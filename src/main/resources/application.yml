spring:
  security:
    user:
      password: user
  datasource:
    url: jdbc:mysql://localhost:3306/polling_app?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    #    properties:
    #      hibernate:
    #        format_sql: true
    hibernate:
      ddl-auto: update